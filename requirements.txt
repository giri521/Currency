# requirements.txt
Flask
gunicorn
requests  # Needed for Perplexity API 
Pillow>=10.4.0 # Or just 'Pillow' to get the latest stable version
python-dotenv
# Use EITHER of the following lines, not both:
# Perplexity version:
perplexity-ai
